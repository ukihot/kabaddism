# セキュリティ

## サポート対象

開発中のため、サポートするのは `main` ブランチの最新コミットのみです。
タグ付きリリースはまだありません。

## 脆弱性の報告

**公開の Issue に書かないでください。**

GitHub の非公開報告を使ってください:
[Security → Report a vulnerability](https://github.com/ukihot/kabaddism/security/advisories/new)

報告には以下を含めてください。

- 影響の内容（何ができてしまうか）
- 再現手順、または該当箇所
- 確認した環境（OS、Rust のバージョン、コミット）

初回の応答は1週間以内を目安とします。個人が余暇で開発しているため、
修正までの期限は約束できません。

## このプロジェクトの性質について

本作は**完全にオフラインのシングルプレイゲーム**で、ネットワーク通信、
アカウント、外部サービスとの連携を一切持ちません（requirements.md §1.1）。
そのため想定される攻撃面は限られますが、以下は報告の対象です。

- 信頼できないセーブファイル（`.ron`）や `assets/data/*.ron` の読み込みによる、
  クラッシュを超える影響（任意コード実行、パストラバーサル、資源の枯渇）
- 依存クレートの既知の脆弱性で、本作の経路から到達可能なもの

以下は脆弱性ではなく、通常の Issue として報告してください。

- 不正なデータでのパニックや、セーブの読み込み失敗
- バランスの破れ、想定外のゲーム内挙動
