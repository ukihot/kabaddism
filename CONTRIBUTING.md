# 貢献の手引き

## 先に読むもの

このプロジェクトは設計書が先にあり、コードはその実装です。変更を出す前に、少なくとも
[doc/design.md](doc/design.md) の §1.1（層分け）と §16（実装フェーズ）に目を通してください。
「なぜこう書かれているのか」の大半はそこに理由が書いてあります。

## 環境

Rust 1.96 以降（edition 2024）。Windows と macOS は `rustup` だけで足ります。
Linux は system ライブラリの開発ヘッダが要ります（README の「動かす」を参照）。

```sh
cargo build                       # 初回は bevy のビルドに10分以上かかります
cargo test
cargo run --release --bin harness -- --years 1 --quiet
```

## 出す前に通すもの

CI と同じものをローカルで回せます。

```sh
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test
sh tools/check_layering.sh
```

`cargo fmt` の設定は `rustfmt.toml` にあります。既定の rustfmt とは異なる
（`use_small_heuristics = "Max"`）ので、エディタの「保存時に整形」も
プロジェクトの設定を読むようにしてください。

## 守ってほしい約束

**`sim/` は bevy を import しない。** これがこのコードベースで唯一の硬い規則です。
`sim` が bevy に依存した瞬間、`App` なしでのテストとヘッドレスのバランス調整が
できなくなり、設計の利点が丸ごと消えます。`tools/check_layering.sh` が確認します。

**`sim` を書き換えるのは限られた場所だけ。** `Game::step_day()` と、プレイヤー操作を
表す少数のコマンド関数（`execute_policy` / `apply_budget` / `cup_run` など）のみ。
`app` 側は `Res<GameRes>` の読み取りに徹します。

**表示文字列をコードに埋め込まない（NFR-10）。** 日本語は `assets/text/ja.ron` に
キーで置き、`defs.text.get("...")` / `format("...", &[...])` で引きます。

**バランス数値をコードに埋め込まない。** `assets/data/balance.ron` に出します。
再ビルドなしで調整できることに価値があります。

**決定論を壊さない（NFR-01 / AC-06）。** 乱数は `sim::rng::RngSet` の用途別ストリームから
取ります。`HashMap` のイテレーション順や、時刻・スレッドに依存する値を
シミュレーションに持ち込まないでください。

## 変更の大きさごとの進め方

| 変更 | 進め方 |
|---|---|
| バランス数値、文言、記事テンプレート | そのまま PR。`harness` の出力がどう変わるかを添えてください |
| バグ修正 | そのまま PR。再現するシードと手順を書いてください |
| UI の追加・変更 | そのまま PR。スクリーンショットを添えてください |
| 新しい政策カード・イベント | そのまま PR。`expect:` の期待値（design.md §15 の政策回帰）も一緒に |
| 日次パイプラインの順序、経済モデル、層分けに関わるもの | **先に Issue で相談してください。** design.md の改訂を伴います |
| 新しい依存クレートの追加 | **先に Issue で相談してください。** NFR-06 により依存は最小に保っています |

## コミットと PR

- 1コミット1論点。整形だけの変更と中身の変更は分けてください。
- コミットメッセージの1行目は、何をしたかを日本語か英語で簡潔に。書式の強制はありません。
- PR には「何を」「なぜ」「どう確かめたか」を書いてください。テンプレートがあります。
- `cargo test` が通り、`harness` が1年完走することを確認してから出してください。

## テストの方針

`sim` は `cargo test` で全部検証できます。design.md §15 に種別の一覧があります。
**UI はテストしません。** 手動で確かめ、スクリーンショットで示してください。

非自明なロジック（分岐・ループ・会計・乱数の使い方）を足したら、それが壊れたときに
落ちる最小のテストを1つ残してください。フレームワークは足さないでください。
